# Excel数据驱动Word文档自动生成工具 - 使用手册

## 目录

1. [项目简介](#项目简介)
2. [快速开始](#快速开始)
3. [Excel数据格式](#excel数据格式)
4. [Word模板格式](#word模板格式)
5. [使用方法](#使用方法)
6. [功能特性](#功能特性)
7. [常见问题](#常见问题)

---

## 项目简介

本工具是一个基于GJB438C标准的软件文档自动生成工具，主要功能包括：

1. **Excel数据驱动Word文档生成**：根据Excel中的测试用例数据，自动在Word测试报告的指定章节生成带动态编号的测试表格
2. **需求分解**：支持将高层需求分解为多个子需求，形成需求树结构
3. **需求追溯**：建立需求与测试用例之间的追溯关系，支持追溯矩阵生成和验证
4. **需求树Word文档生成**：将需求树以树状结构输出到Word文档

### 核心特性

- ✅ **零侵入性**：完全适配原始Word模板，无需修改模板结构
- ✅ **精准定位**：按Word章节标题自动定位填充位置
- ✅ **动态编号**：模块内编号自动生成（如5.3.1、5.3.2）
- ✅ **批量处理**：支持一次处理多个模块数据
- ✅ **格式保留**：完全保留原始模板的格式，自动提取并应用模板格式

---

## 快速开始

### 1. 系统要求

- JDK 8 及以上
- Maven 3.6 及以上（用于构建）
- Word 2016及以上（.docx格式）
- Excel 2016及以上（.xlsx格式）

### 2. 构建项目

```bash
mvn clean package
```

构建完成后，在 `target` 目录下会生成 `DocAutoGenByExcel-0.0.1-SNAPSHOT.jar` 文件。

### 3. 准备数据文件

#### Excel数据格式

Excel文件必须包含以下列（列名不可修改，顺序可调整）：

| 列名 | 是否必填 | 说明 | 示例 |
|------|---------|------|------|
| 模块编号 | 是 | Word章节对应前缀 | 5.3、6.2 |
| testName | 是 | 测试名称 | 功能1、接口A |
| id | 是 | 测试用例唯一标识 | F001、I002 |
| content | 是 | 测试内容 | 验证功能1的登录逻辑 |
| strategy | 是 | 测试策略与方法 | 1) 模拟正常登录；2) 输入错误密码 |
| criteria | 是 | 判定准则 | 1) 登录成功跳转首页 |
| stopCondition | 是 | 测试终止条件 | 测试用例执行完成 |
| trace | 是 | 追踪关系 | 需求文档V1.0 |

**注意事项：**
- 第一行为列名
- 支持空行自动过滤
- 单元格数据超过500字符正常读取，Word中自动换行

#### Word模板格式

Word模板中需要包含章节标题，格式为：`X.X 模块名称`

例如：
- `5.3 功能测试`
- `6.2 接口测试`

章节标题必须是独立段落，文本内容完全匹配上述格式。

**模板子章节格式要求：**
- 模板中应包含至少一个子章节示例（如 `5.2.1 XX测试`），用于提取格式
- 子章节标题格式：
  - 编号部分：黑体 10号字体
  - 内容部分：黑体 12pt（小四号）
- 表格标题（Caption）格式：黑体 12pt（小四号）

### 4. 运行工具

#### 方式一：命令行参数

```bash
java -jar target/DocAutoGenByExcel-0.0.1-SNAPSHOT.jar -excel "test_data.xlsx" -word "test_template.docx" -out "output"
```

参数说明：
- `-excel`: Excel文件路径（必填）
- `-word`: Word模板文件路径（必填）
- `-out`: 输出目录路径（可选，默认为Excel文件同目录）
- `-h`: 显示帮助信息

#### 方式二：配置文件

1. 复制 `src/main/resources/config.properties.example` 为 `config.properties`
2. 编辑配置文件，填写实际路径：

```properties
excel.path=D:/data.xlsx
word.path=D:/template.docx
output.path=D:/output
```

3. 运行：

```bash
java -jar DocAutoGenByExcel-0.0.1-SNAPSHOT.jar -config
```

或直接运行（无参数时自动使用配置文件）：

```bash
java -jar DocAutoGenByExcel-0.0.1-SNAPSHOT.jar
```

### 5. 输出结果

生成的Word文件命名规则：`原始模板名_生成时间.docx`

例如：`测试报告模板_202511281530.docx`

---

## Excel数据格式

### 必需列

| 列名 | 说明 | 示例 |
|------|------|------|
| 模块编号 | Word章节对应前缀，格式为 X.X | 5.3、6.2 |
| testName | 测试名称，将用于生成子章节标题 | 登录功能、注册功能 |
| id | 测试用例唯一标识 | F001、I002 |
| content | 测试内容描述 | 验证用户登录功能 |
| strategy | 测试策略与方法 | 1) 输入正确用户名密码<br>2) 点击登录按钮 |
| criteria | 判定准则 | 1) 登录成功<br>2) 跳转到首页 |
| stopCondition | 测试终止条件 | 测试用例执行完成 |
| trace | 追踪关系 | 需求文档V1.0 |

### 数据要求

- 第一行必须是列名
- 支持空行，会自动过滤
- 单元格内容支持多行文本（使用换行符）
- 单元格数据超过500字符正常读取，Word中自动换行

### 示例数据

```
模块编号 | testName | id | content | strategy | criteria | stopCondition | trace
5.2 | 登录功能 | TC001 | 验证登录功能 | 1)输入用户名密码<br>2)点击登录 | 登录成功 | 测试完成 | REQ-001
5.2 | 注册功能 | TC002 | 验证注册功能 | 1)输入注册信息<br>2)提交 | 注册成功 | 测试完成 | REQ-002
```

---

## Word模板格式

### 章节标题格式

Word模板中需要包含章节标题，格式为：`X.X 模块名称`

- `X.X` 为数字格式的章节编号（如 5.2、5.3）
- 章节标题必须是独立段落
- 文本内容必须完全匹配上述格式

### 子章节模板

模板中应包含至少一个子章节示例，用于格式提取：

**格式要求：**
- 子章节标题（Heading 3样式）：
  - 编号部分（如 `5.2.1 `）：黑体 10号字体
  - 内容部分（如 `XX测试`）：黑体 12pt（小四号）
- 表格标题（Caption样式）：
  - 全部内容：黑体 12pt（小四号）

**示例：**
```
5.2 功能测试

5.2.1 XX测试

表5.2.1 XX测试

[表格内容]
```

### 表格模板

- 模板中应包含至少一个表格作为模板
- 表格结构会被复制到新生成的子章节
- 表格数据会根据Excel内容自动填充

---

## 使用方法

### 基本使用流程

1. **准备Excel数据文件**
   - 按照格式要求填写测试用例数据
   - 确保模块编号与Word模板中的章节编号对应

2. **准备Word模板文件**
   - 确保模板中包含对应的章节标题
   - 建议包含至少一个子章节示例用于格式提取

3. **运行工具**
   ```bash
   java -jar target/DocAutoGenByExcel-0.0.1-SNAPSHOT.jar -excel "data.xlsx" -word "template.docx" -out "output"
   ```

4. **检查输出结果**
   - 在输出目录查看生成的Word文档
   - 验证格式和数据是否正确

### 生成的表格格式

工具会在Word指定章节下生成以下格式的表格：

| 测试项名称 | testName值 | 标识 | id值 |
|-----------|-----------|------|------|
| 测试内容 | content值 | | |
| 测试策略与方法 | strategy值 | | |
| 判定准则 | criteria值 | | |
| 测试终止条件 | stopCondition值 | | |
| 追踪关系 | trace值 | | |

每个测试用例会生成：
1. 子标题：`模块编号.序号 测试名称测试`（如：`5.3.1 功能1测试`）
2. 表格标题（Caption）：`表模块编号.序号 测试名称测试`
3. 测试表格（6行4列）
4. 空行（最后一个测试用例后无空行）

### 格式自动提取

工具会自动从模板中提取格式：

1. **子章节格式提取**：
   - 从第一个已存在的子章节段落提取格式
   - 自动识别编号部分和内容部分的格式差异
   - 应用到所有新生成的子章节

2. **Caption格式提取**：
   - 从子章节后的Caption段落提取格式
   - 应用到所有新生成的表格标题

3. **格式应用**：
   - 替换已存在的子章节时，保持原有格式
   - 创建新子章节时，使用提取的模板格式

---

## 功能特性

### 1. Excel到Word文档生成

- 从Excel读取测试用例数据
- 自动填充Word模板表格
- 支持多模块批量处理
- 自动生成子章节编号

### 2. 需求分解

- 支持手动和自动需求分解
- 自动生成需求编号
- 支持多级需求分解
- 需求树可视化

### 3. 需求追溯

- 建立需求与测试用例的追溯关系
- 生成追溯矩阵
- 追溯关系验证和覆盖率计算
- 支持Excel集成

### 4. 需求树Word文档生成

- 生成树状结构的需求树文档
- 支持显示追溯信息
- 自动生成统计信息
- 自定义格式支持

---

## 常见问题

### 1. Excel文件读取失败

**问题**：提示"Excel文件路径错误或文件损坏"

**解决**：
- 确认文件路径正确
- 确认文件格式为.xlsx（不支持.xls）
- 确认文件未被其他程序占用

### 2. Word模板中未找到模块

**问题**：提示"Word模板中未找到模块: X.X"

**解决**：
- 确认Word模板中存在对应章节标题
- 确认章节标题格式为：`X.X 模块名称`（X为数字）
- 确认章节标题为独立段落
- 检查是否在目录（TOC）中，工具会自动跳过目录项

### 3. Excel缺少必填列

**问题**：提示"Excel缺少必填列: XXX"

**解决**：
- 确认Excel第一行为列名
- 确认列名拼写正确（区分大小写）
- 参考"Excel数据格式"章节检查列名

### 4. 生成的表格格式不正确

**问题**：表格边框、对齐方式不符合要求

**解决**：
- 确认使用Word 2016及以上版本打开
- 如仍有问题，请检查Word模板格式
- 确认模板中包含正确的表格模板

### 5. 格式不符合模板要求

**问题**：生成的子章节或Caption格式与模板不一致

**解决**：
- 确认模板中包含至少一个子章节示例
- 确认模板子章节使用了正确的格式（黑体、字号等）
- 工具会自动提取模板格式，如果提取失败会使用默认格式

### 6. 不支持.doc格式

**问题**：提示"不支持旧版Word格式(.doc文件)"

**解决**：
- 使用Microsoft Word打开.doc文件
- 选择"文件" -> "另存为"
- 在"文件类型"中选择"Word文档(*.docx)"
- 保存后使用新的.docx文件

---

## 性能指标

- 数据处理能力：支持单次处理100条以内测试用例
- 处理时间：100条数据处理时间≤10秒
- 内存占用：运行时内存占用≤512MB

---

*文档版本：v1.0*  
*最后更新：2025-12-10*

